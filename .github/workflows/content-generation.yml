name: Content Generation

on:
  schedule:
    # Run every Sunday at 08:00 UTC
    - cron: '0 8 * * 0'
  workflow_dispatch:
    inputs:
      channel:
        description: 'Specific channel to generate (leave empty for all)'
        required: false
        default: ''
        type: choice
        options:
          - ''
          - reddit
          - linkedin
          - x-thread
          - instagram
          - outreach
          - community
          - blog

permissions:
  contents: write

jobs:
  detect-trends:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Run trend detection
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: node agents/trend-detector.js
        continue-on-error: true

      - name: Upload trend signals
        uses: actions/upload-artifact@v4
        with:
          name: trend-signals
          path: content/signals/
          retention-days: 30

  generate-content:
    needs: detect-trends
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Download trend signals
        uses: actions/download-artifact@v4
        with:
          name: trend-signals
          path: content/signals/
        continue-on-error: true

      - name: Generate content
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          if [ -n "${{ github.event.inputs.channel }}" ]; then
            node agents/engine.js --channel "${{ github.event.inputs.channel }}"
          else
            node agents/engine.js
          fi

      - name: Commit and push content
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add content/ web/blog/
          if git diff --staged --quiet; then
            echo "No new content to commit"
          else
            DATE=$(date +%Y-%m-%d)
            git commit -m "content: auto-generated weekly content ($DATE)"
            git pull --rebase origin master
            git push
          fi

      - name: 🚀 Trigger Make.com Distribution
        env:
          MAKECOM_WEBHOOK_URL: ${{ secrets.MAKECOM_WEBHOOK_URL }}
        run: |
          cat << 'EOF' > dummy-payload.json
          {
            "linkedin_ready": true,
            "linkedin_text": "Ini adalah contoh post LinkedIn dari Vect AI.",
            "twitter_ready": true,
            "twitter_text": "Ini adalah contoh tweet dari Vect AI.",
            "instagram_ready": true,
            "instagram_text": "Ini adalah contoh caption IG dari Vect AI.",
            "outreach_ready": true,
            "outreach_target": "CEO Startup",
            "outreach_subject": "Tanya soal verifikasi data",
            "outreach_body": "Halo, saya dari Vect..."
          }
          EOF
          
          echo "Sending payload to Make.com..."
          curl -X POST -H "Content-Type: application/json" -d @dummy-payload.json $MAKECOM_WEBHOOK_URL

  notify-on-failure:
    needs: generate-content
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Create failure issue
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Content Generation Failed - ${new Date().toISOString().split('T')[0]}`,
              body: `The weekly content generation workflow failed.\n\nRun: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}\n\nPlease investigate and manually generate content if needed.`,
              labels: ['content-generation-failure']
            });
