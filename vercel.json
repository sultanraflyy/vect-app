{
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npm install --legacy-peer-deps && npx --yes expo@54 export --platform web --output-dir $(pwd)/dist",
  "outputDirectory": "dist",
  "framework": null
}
